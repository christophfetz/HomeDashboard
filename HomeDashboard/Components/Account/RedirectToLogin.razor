@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // WICHTIG: nur umleiten, wenn wir NICHT schon auf /login sind
        if (!Navigation.Uri.EndsWith("/login"))
        {
            Navigation.NavigateTo("/login", true);
        }
    }
}